package com.oni.udash.views

import org.scalatest.FunSpec
import org.scalatest.Matchers
import org.scalatest.BeforeAndAfter

class ComingSoonViewTest extends FunSpec
    with Matchers
    with BeforeAndAfter {
  
  describe("test rendering of coming soon page") {
    it("should render with styles") {
      println("here is: ")
//      val testFile = "cloud_plan/cloud_ocr/src/test/resources/com/globalvelocity/ocr/policy-v1.tiff"
//      val ocrf = new OcrFile(Left(testFile))
//      ocrf.tmpFile.length() should be(25248998)
//      println("tmp file = " + ocrf.tmpFile)
//      ocrf.close()
//      ocrf.tmpFile.exists() should be(false)
    }
  }
}